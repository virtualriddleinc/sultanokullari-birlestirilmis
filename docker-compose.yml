services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload
      POSTGRES_DB: sultanokullari
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
